import { ComponentFactoryResolver, ComponentRef, Injector, RendererFactory2 } from '@angular/core';
import { Router } from '@angular/router';
import { Actions } from '@ngxs/store';
import { HttpErrorWrapperComponent } from '../components/http-error-wrapper/http-error-wrapper.component';
import { ErrorScreenErrorCodes, HttpErrorConfig } from '../models/common';
import { ConfirmationService } from '../services/confirmation.service';
export declare const DEFAULT_ERROR_MESSAGES: {
    defaultError: {
        title: string;
        details: string;
    };
    defaultError401: {
        title: string;
        details: string;
    };
    defaultError403: {
        title: string;
        details: string;
    };
    defaultError404: {
        title: string;
        details: string;
    };
    defaultError500: {
        title: string;
        details: string;
    };
};
export declare const DEFAULT_ERROR_LOCALIZATIONS: {
    defaultError: {
        title: string;
        details: string;
    };
    defaultError401: {
        title: string;
        details: string;
    };
    defaultError403: {
        title: string;
        details: string;
    };
    defaultError404: {
        title: string;
        details: string;
    };
    defaultError500: {
        title: string;
        details: string;
    };
};
export declare class ErrorHandler {
    private actions;
    private router;
    private confirmationService;
    private cfRes;
    private rendererFactory;
    private injector;
    private httpErrorConfig;
    componentRef: ComponentRef<HttpErrorWrapperComponent>;
    constructor(actions: Actions, router: Router, confirmationService: ConfirmationService, cfRes: ComponentFactoryResolver, rendererFactory: RendererFactory2, injector: Injector, httpErrorConfig: HttpErrorConfig);
    private listenToRouterError;
    private listenToRouterDataResolved;
    private listenToRestError;
    private show401Page;
    private show404Page;
    private showError;
    private navigateToLogin;
    createErrorComponent(instance: Partial<HttpErrorWrapperComponent>): void;
    canCreateCustomError(status: ErrorScreenErrorCodes): boolean;
    private filterRestErrors;
    private filterRouteErrors;
}
