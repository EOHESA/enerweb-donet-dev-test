import { OnInit, Injector } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { ActivatedRoute } from '@angular/router';
import { ToasterService } from '@abp/ng.theme.shared';
import { TextTemplateContentDto } from '../../proxy/text-templates/models';
import { TemplateContentService } from '../../proxy/text-templates/template-content.service';
export declare class AbstractTemplateContentComponent implements OnInit {
    injector: Injector;
    protected fb: FormBuilder;
    protected templateContentService: TemplateContentService;
    protected route: ActivatedRoute;
    protected toaster: ToasterService;
    form: FormGroup;
    templateContent: TextTemplateContentDto;
    busy: boolean;
    selectedCultureName: string;
    constructor(injector: Injector);
    ngOnInit(): void;
    getData(): import("rxjs").Observable<TextTemplateContentDto>;
    save(callback?: () => any): void;
    restoreToDefault(): void;
}
