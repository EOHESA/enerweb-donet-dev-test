import { ABP, ListService, PagedResultDto } from '@abp/ng.core';
import { OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { TemplateDefinitionDto } from '../../proxy/text-templates/models';
export declare class TextTemplatesComponent implements OnInit {
    readonly list: ListService<ABP.PageQueryParams>;
    private store;
    private router;
    pagedData$: Observable<PagedResultDto<TemplateDefinitionDto>>;
    constructor(list: ListService<ABP.PageQueryParams>, store: Store, router: Router);
    ngOnInit(): void;
    private hookToQuery;
    editContents(record: TemplateDefinitionDto): void;
}
