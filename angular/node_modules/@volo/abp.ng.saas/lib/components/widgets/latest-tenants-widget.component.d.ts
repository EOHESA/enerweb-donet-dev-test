import { PermissionService } from '@abp/ng.core';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { SaasTenantDto } from '../../proxy/host/dtos/models';
export declare class LatestTenantsWidgetComponent {
    private store;
    private permissionService;
    datas$: Observable<SaasTenantDto[]>;
    minHeight: number;
    trackByFn: (_: any, item: any) => any;
    constructor(store: Store, permissionService: PermissionService);
    draw: () => void;
}
