import { ListService } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { Injector, OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { EditionDto, GetEditionsInput } from '../../proxy/host/dtos/models';
import { EditionService } from '../../proxy/host/edition.service';
export declare class EditionsComponent implements OnInit {
    readonly list: ListService<GetEditionsInput>;
    private confirmationService;
    private editionService;
    private store;
    private injector;
    data$: Observable<EditionDto[]>;
    totalCount$: Observable<number>;
    selected: EditionDto;
    editionForm: FormGroup;
    isModalVisible: boolean;
    visibleFeatures: boolean;
    providerKey: string;
    modalBusy: boolean;
    onVisibleFeaturesChange: (value: boolean) => void;
    constructor(list: ListService<GetEditionsInput>, confirmationService: ConfirmationService, editionService: EditionService, store: Store, injector: Injector);
    ngOnInit(): void;
    private hookToQuery;
    private createEditionForm;
    onAddEdition(): void;
    onEditEdition(id: string): void;
    save(): void;
    delete({ id, displayName }: EditionDto): void;
    openFeaturesModal(providerKey: string): void;
}
