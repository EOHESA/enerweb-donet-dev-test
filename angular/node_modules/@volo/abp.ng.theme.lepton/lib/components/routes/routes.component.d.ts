import { ABP, RoutesService, TreeNode } from '@abp/ng.core';
import { EventEmitter, Injector, OnInit, TrackByFunction } from '@angular/core';
import { Router } from '@angular/router';
export declare class RoutesComponent implements OnInit {
    private router;
    readonly routes: RoutesService;
    readonly injector: Injector;
    clickedToLink: EventEmitter<MouseEvent>;
    isMenuPlacementTop: boolean;
    smallScreen: boolean;
    readonly expandedRoutes: Set<string>;
    readonly initialLevel = 1;
    trackByFn: TrackByFunction<TreeNode<ABP.Route>>;
    filterOp: import("rxjs").OperatorFunction<TreeNode<ABP.Route>[], TreeNode<ABP.Route>[]>;
    get routes$(): import("rxjs").Observable<TreeNode<ABP.Route>[]>;
    constructor(router: Router, routes: RoutesService, injector: Injector);
    isDropdown(node: TreeNode<ABP.Route>): boolean;
    ngOnInit(): void;
    onNavigate({ parentName }: ABP.Route, level: number): void;
    toggleExpand({ name }: ABP.Route, level: number): void;
    collapseDropdowns(name: string, level: number): void;
}
