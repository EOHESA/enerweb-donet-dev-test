import { AfterViewInit, ElementRef, EventEmitter, Renderer2 } from '@angular/core';
import { eThemeLeptonComponents } from '../../enums';
import { SubscriptionService } from '@abp/ng.core';
import { Store } from '@ngxs/store';
import { LayoutStateService } from '../../services/layout-state.service';
export declare class HeaderComponent implements AfterViewInit {
    private subscription;
    private renderer;
    private store;
    private layoutStateService;
    isNavbarExpanded$: import("rxjs").Observable<boolean>;
    isMenuExpanded$: import("rxjs").Observable<boolean>;
    smallScreen$: import("rxjs").Observable<boolean>;
    isMenuPlacementTop$: import("rxjs").Observable<boolean>;
    logoComponentKey: eThemeLeptonComponents;
    navbarComponentKey: eThemeLeptonComponents;
    navbarMobileComponentKey: eThemeLeptonComponents;
    sidebarComponentKey: eThemeLeptonComponents;
    mouseMoveContainer: EventEmitter<ElementRef<HTMLElement>[]>;
    navbarBrandRef: ElementRef<any>;
    constructor(subscription: SubscriptionService, renderer: Renderer2, store: Store, layoutStateService: LayoutStateService);
    ngAfterViewInit(): void;
    navbarIconClick(isNavbarExpanded: boolean): void;
    menuIconClick(isMenuExpanded: boolean): void;
}
