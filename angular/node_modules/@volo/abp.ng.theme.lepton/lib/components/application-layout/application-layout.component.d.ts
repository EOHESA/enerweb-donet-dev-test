import { ApplicationInfo, eLayoutType, EnvironmentService } from '@abp/ng.core';
import { ElementRef, OnDestroy } from '@angular/core';
import { eThemeLeptonComponents } from '../../enums/components';
import { LayoutService } from '../../services/layout.service';
import { BehaviorSubject, Subscription } from 'rxjs';
import { LayoutStateService } from '../../services/layout-state.service';
export declare class ApplicationLayoutComponent implements OnDestroy {
    private environment;
    private layoutState;
    private layout;
    static type: eLayoutType;
    headerComponentKey: eThemeLeptonComponents;
    footerComponentKey: eThemeLeptonComponents;
    mouseMoveSubscription: Subscription;
    containers$: BehaviorSubject<ElementRef<HTMLElement>[]>;
    get appInfo(): ApplicationInfo;
    constructor(environment: EnvironmentService, layoutState: LayoutStateService, layout: LayoutService);
    listenMouseMove(containerRefs: ElementRef<HTMLElement>[]): void;
    ngOnDestroy(): void;
}
