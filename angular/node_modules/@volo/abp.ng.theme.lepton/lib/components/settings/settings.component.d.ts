import { SubscriptionService } from '@abp/ng.core';
import { ToasterService } from '@abp/ng.theme.shared';
import { AfterViewInit, OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { Layout } from '../../models/layout';
import { LayoutStateService } from '../../services/layout-state.service';
import { FormBuilder, FormGroup } from '@angular/forms';
import { LayoutService } from '../../services/layout.service';
export declare class SettingsComponent implements OnInit, AfterViewInit {
    private toaster;
    private router;
    private subscription;
    private layoutStateService;
    private layout;
    private fb;
    placements: {
        Left: Layout.MenuPlacement;
        Top: Layout.MenuPlacement;
    };
    statuses: {
        AlwaysOpened: Layout.MenuStatus;
        OpenOnHover: Layout.MenuStatus;
    };
    boxedLayout: boolean;
    loading: boolean;
    viewInitialized: boolean;
    form: FormGroup;
    get isSmallScreen(): boolean;
    constructor(toaster: ToasterService, router: Router, subscription: SubscriptionService, layoutStateService: LayoutStateService, layout: LayoutService, fb: FormBuilder);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    save(): void;
}
