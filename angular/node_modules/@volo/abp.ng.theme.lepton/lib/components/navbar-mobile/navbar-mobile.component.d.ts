import { EventEmitter } from '@angular/core';
export declare class NavbarMobileComponent {
    isNavbarExpanded: boolean;
    isMenuExpanded: boolean;
    navbarIconClick: EventEmitter<boolean>;
    menuIconClick: EventEmitter<boolean>;
}
