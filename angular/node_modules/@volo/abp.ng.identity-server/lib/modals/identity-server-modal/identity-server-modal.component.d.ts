import { OnInit, EventEmitter, QueryList } from '@angular/core';
import { IdentityServerModalTabComponent } from './tabs/identity-server-modal-tab.component';
export declare class IdentityServerModalComponent implements OnInit {
    type: 'Create' | 'Edit';
    visible: boolean;
    visibleChange: EventEmitter<boolean>;
    save: EventEmitter<any>;
    tabs: QueryList<IdentityServerModalTabComponent>;
    activeTab: string;
    constructor();
    ngOnInit(): void;
    onVisibleChange(event: boolean): void;
    shouldBeDisabled: () => boolean;
    onSave(): void;
}
