import { OnInit } from '@angular/core';
import { ListService, ABP, PagedResultDto } from '@abp/ng.core';
import { Observable } from 'rxjs';
export declare type EntityService<T> = {
    getList: (query: ABP.PageQueryParams) => Observable<PagedResultDto<T>>;
};
export declare class IdentityServerEntityBaseComponent<T> implements OnInit {
    entityName: string;
    list: ListService;
    service: EntityService<T>;
    data$: Observable<PagedResultDto<T>>;
    totalCount: any;
    constructor();
    ngOnInit(): void;
    private hookToQuery;
}
