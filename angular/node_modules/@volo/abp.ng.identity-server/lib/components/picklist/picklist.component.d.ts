import { AbstractNgModelComponent } from '@abp/ng.core';
import { TrackByFunction, Injector } from '@angular/core';
export declare type PicklistDataType<T = {
    [key: string]: any;
}> = Array<T & {
    left: boolean;
}>;
export declare class PicklistComponent extends AbstractNgModelComponent<PicklistDataType> {
    injector: Injector;
    leftListTitle: string;
    rightListTitle: string;
    trackByProp: string;
    bindLabel: string;
    constructor(injector: Injector);
    trackByFn(): TrackByFunction<any>;
    transferItem(label: string): void;
}
