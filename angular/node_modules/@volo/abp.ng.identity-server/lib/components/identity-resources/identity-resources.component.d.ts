import { ListService } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { Injector, OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { IdentityResourcesService } from '../../proxy/identity-resources.service';
import { GetIdentityResourceListInput, IdentityResourceWithDetailsDto, IdentityResourcePropertyDto } from '../../proxy/identity-resource/dtos/models';
import { IdentityServerClaimModel } from '../../modals/identity-server-modal/tabs/identity-server-modal-claims-tab/identity-server-modal-claims-tab.component';
export declare class IdentityResourcesComponent implements OnInit {
    readonly list: ListService<GetIdentityResourceListInput>;
    readonly service: IdentityResourcesService;
    private confirmationService;
    private fb;
    private injector;
    createModalVisible: boolean;
    newScopeForm: FormGroup;
    editModalVisible: boolean;
    editScopeForm: FormGroup;
    editClaims: IdentityServerClaimModel[];
    editSelectedRecord: IdentityResourceWithDetailsDto;
    editProperties: IdentityResourcePropertyDto[];
    constructor(list: ListService<GetIdentityResourceListInput>, service: IdentityResourcesService, confirmationService: ConfirmationService, fb: FormBuilder, injector: Injector);
    ngOnInit(): void;
    onAdd(): void;
    create(formValue: any): void;
    onEdit(id: string): void;
    edit(formValue: any): void;
    onCreateStandardResources(): void;
    delete(id: string, name: string): void;
}
