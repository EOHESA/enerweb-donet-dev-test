import { ListService } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { Injector, OnInit, TemplateRef, TrackByFunction } from '@angular/core';
import { AbstractControl, FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { ClaimTypeDto, GetIdentityClaimTypesInput } from '../../proxy/identity/models';
export declare class ClaimsComponent implements OnInit {
    readonly list: ListService<GetIdentityClaimTypesInput>;
    protected confirmationService: ConfirmationService;
    protected store: Store;
    protected injector: Injector;
    data$: Observable<ClaimTypeDto[]>;
    totalCount$: Observable<number>;
    modalContent: TemplateRef<any>;
    form: FormGroup;
    selected: ClaimTypeDto;
    isModalVisible: boolean;
    modalBusy: boolean;
    trackByFn: TrackByFunction<AbstractControl>;
    constructor(list: ListService<GetIdentityClaimTypesInput>, confirmationService: ConfirmationService, store: Store, injector: Injector);
    ngOnInit(): void;
    private hookToQuery;
    buildForm(): void;
    getTypeName(valueType: number): number | "String" | "Int" | "Boolean" | "DateTime";
    openModal(): void;
    onAdd(): void;
    onEdit(id: string): void;
    save(): void;
    delete(id: string, name: string): void;
}
