import { ListService } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { Injector, OnInit, TemplateRef } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { IdentityRoleService } from '../../proxy/identity/identity-role.service';
import { GetIdentityRoleListInput, IdentityRoleDto } from '../../proxy/identity/models';
export declare class RolesComponent implements OnInit {
    readonly list: ListService<GetIdentityRoleListInput>;
    protected confirmationService: ConfirmationService;
    protected roleService: IdentityRoleService;
    protected store: Store;
    protected injector: Injector;
    data$: Observable<IdentityRoleDto[]>;
    totalCount$: Observable<number>;
    form: FormGroup;
    selected: IdentityRoleDto;
    isModalVisible: boolean;
    visiblePermissions: boolean;
    providerKey: string;
    modalBusy: boolean;
    visibleClaims: boolean;
    claimSubject: {
        id: string;
        type: 'roles' | 'users';
    };
    modalContent: TemplateRef<any>;
    onVisiblePermissionChange: (value: boolean) => void;
    constructor(list: ListService<GetIdentityRoleListInput>, confirmationService: ConfirmationService, roleService: IdentityRoleService, store: Store, injector: Injector);
    private createForm;
    private hookToQuery;
    ngOnInit(): void;
    openModal(): void;
    onAdd(): void;
    onEdit(id: string): void;
    save(): void;
    delete(id: string, name: string): void;
    onManageClaims(id: string): void;
    openPermissionsModal(providerKey: string): void;
}
