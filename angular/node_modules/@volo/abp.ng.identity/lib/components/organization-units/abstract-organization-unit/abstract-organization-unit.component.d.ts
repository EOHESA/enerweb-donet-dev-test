import { ABP, ListService, PagedResultDto } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { InjectionToken, Injector, OnChanges, SimpleChanges } from '@angular/core';
import { IdentityRoleDto, IdentityUserDto, OrganizationUnitWithDetailsDto } from '../../../proxy/identity/models';
import { OrganizationUnitService } from '../../../proxy/identity/organization-unit.service';
export interface OrganizationUnitConfig {
    getCurrentUnitsMethodName: string;
    addUnitsMethodName: string;
    addUnitsBodyPropName: string;
    deleteMethodName: string;
    deletionLocalizationKey: string;
}
export declare const ORGANIZATION_UNIT_CONFIG: InjectionToken<any>;
export declare class AbstractOrganizationUnitComponent<T = IdentityUserDto | IdentityRoleDto> implements OnChanges {
    protected injector: Injector;
    selectedOrganizationUnit: OrganizationUnitWithDetailsDto;
    currentOrganizationUnits: PagedResultDto<T>;
    checkedUnits: ABP.Dictionary<boolean>;
    isModalVisible: boolean;
    isModalBusy: boolean;
    readonly list: ListService;
    protected readonly organizationUnitService: OrganizationUnitService;
    protected readonly confirmation: ConfirmationService;
    protected readonly config: OrganizationUnitConfig;
    constructor(injector: Injector);
    ngOnChanges({ selectedOrganizationUnit }: SimpleChanges): void;
    private hookToQuery;
    addUnits(): void;
    delete(unitId: string, unitName: string): void;
    openModal(): void;
    isCheckboxDisabled: (id: string) => boolean;
}
