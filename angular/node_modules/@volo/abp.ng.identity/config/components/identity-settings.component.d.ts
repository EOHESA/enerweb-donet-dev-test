import { ApplicationConfigurationService, ConfigStateService, SubscriptionService } from '@abp/ng.core';
import { ToasterService } from '@abp/ng.theme.shared';
import { FormBuilder, FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import type * as IdentitySettings from '../models/identity-settings';
import { IdentityConfigService } from '../services/identity-config.service';
export declare class IdentitySettingsComponent {
    private identityConfigService;
    private toaster;
    private fb;
    private store;
    private subscription;
    private configState;
    private appConfigService;
    settings: IdentitySettings.Settings;
    loading: boolean;
    form: FormGroup;
    get initialized(): boolean;
    doNotRequireConfirmedPhoneNumber: () => void;
    enablePhoneNumberConfirmation: () => void;
    constructor(identityConfigService: IdentityConfigService, toaster: ToasterService, fb: FormBuilder, store: Store, subscription: SubscriptionService, configState: ConfigStateService, appConfigService: ApplicationConfigurationService);
    buildForm(): void;
    syncConfirmationOptions(): void;
    submit(): void;
}
