import { ConfigStateService } from '@abp/ng.core';
import { Injector } from '@angular/core';
import { ProfilePictureService } from '../services/profile-picture.service';
export declare class ProfilePictureHandler {
    private profilePictureService;
    private injector;
    private configState;
    private currentUserId;
    constructor(profilePictureService: ProfilePictureService, injector: Injector, configState: ConfigStateService);
    private listenToAppConfig;
}
