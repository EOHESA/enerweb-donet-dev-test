import { RestService } from '@abp/ng.core';
import { ProfilePictureInput, ProfilePictureSourceDto } from '../models/profile-picture';
export declare class ProfilePictureService {
    private restService;
    protected url: string;
    publicApiName: string;
    getProfilePicture: (id: string, skipHandleError?: boolean) => import("rxjs").Observable<ProfilePictureSourceDto>;
    setProfilePicture: (input: ProfilePictureInput) => import("rxjs").Observable<void>;
    constructor(restService: RestService);
}
