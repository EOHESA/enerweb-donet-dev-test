import { ContentProjectionService } from '@abp/ng.core';
import { AuditLogsService } from '@volo/abp.ng.audit-logging';
export declare class EntityChangeModalService {
    private auditLogsService;
    private contentProjectionService;
    private modalRef;
    constructor(auditLogsService: AuditLogsService, contentProjectionService: ContentProjectionService);
    private projectModal;
    detectChanges(): void;
    showDetails(entityChangeId: string): void;
    showHistory(entityId: string, entityTypeFullName: string): void;
}
