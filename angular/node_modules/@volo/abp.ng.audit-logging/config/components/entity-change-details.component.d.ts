import { TrackByService } from '@abp/ng.core';
import { EntityChangeDto, EntityChangeWithUsernameDto, EntityPropertyChangeDto } from '@volo/abp.ng.audit-logging';
export declare class EntityChangeDetailsComponent {
    readonly track: TrackByService<EntityPropertyChangeDto>;
    set itemWithUserName(item: EntityChangeWithUsernameDto);
    isCollapsed: boolean;
    entityChange: EntityChangeDto;
    userName: string;
    changeType: string;
    color: 'success' | 'info' | 'danger';
    private colors;
    uuid: string;
    constructor(track: TrackByService<EntityPropertyChangeDto>);
    getPropColor(propertyChange: EntityPropertyChangeDto): 'red' | undefined;
    getInterpolationParams(changeTime: string): string[];
}
