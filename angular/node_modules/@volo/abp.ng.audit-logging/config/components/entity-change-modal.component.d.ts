import { TrackByService } from '@abp/ng.core';
import { ChangeDetectorRef } from '@angular/core';
import { EntityChangeWithUsernameDto } from '@volo/abp.ng.audit-logging';
export declare class EntityChangeModalComponent {
    readonly cdRef: ChangeDetectorRef;
    readonly track: TrackByService;
    history: EntityChangeWithUsernameDto[];
    entityId: string;
    entityTypeFullName: string;
    visible: boolean;
    constructor(cdRef: ChangeDetectorRef, track: TrackByService);
}
