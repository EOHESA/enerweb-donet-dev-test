import { PermissionService, SubscriptionService } from '@abp/ng.core';
import { ChartComponent, Statistics } from '@abp/ng.theme.shared';
import { Store } from '@ngxs/store';
import { BehaviorSubject, Observable } from 'rxjs';
export declare class ErrorRateWidgetComponent {
    private store;
    private subscription;
    private permissionService;
    data$: Observable<Statistics.Data>;
    initialized: BehaviorSubject<this>;
    width: number;
    height: number;
    chart: ChartComponent;
    chartData: any;
    draw: (filter: {
        startDate: string;
        endDate: string;
    }) => void;
    initialize(chartComponent: ChartComponent): void;
    constructor(store: Store, subscription: SubscriptionService, permissionService: PermissionService);
    listenToData(): void;
}
