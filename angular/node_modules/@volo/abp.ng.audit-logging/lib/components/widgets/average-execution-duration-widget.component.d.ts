import { LocalizationService, PermissionService, SubscriptionService } from '@abp/ng.core';
import { ChartComponent, Statistics } from '@abp/ng.theme.shared';
import { Store } from '@ngxs/store';
import { BehaviorSubject, Observable } from 'rxjs';
export declare class AverageExecutionDurationWidgetComponent {
    private store;
    private localizationService;
    private subscription;
    private permissionService;
    data$: Observable<Statistics.Data>;
    initialized: BehaviorSubject<this>;
    width: number;
    height: number;
    chart: ChartComponent;
    chartData: any;
    draw: (filter: {
        startDate: string;
        endDate: string;
    }) => void;
    constructor(store: Store, localizationService: LocalizationService, subscription: SubscriptionService, permissionService: PermissionService);
    initialize(chartComponent: ChartComponent): void;
    listenToData(): void;
}
