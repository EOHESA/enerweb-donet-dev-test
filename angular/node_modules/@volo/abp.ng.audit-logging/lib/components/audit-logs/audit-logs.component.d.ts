import { ListService } from '@abp/ng.core';
import { OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { AuditLogsService } from '../../proxy/audit-logging/audit-logs.service';
import { AuditLogDto, GetAuditLogListDto } from '../../proxy/audit-logging/models';
export declare class AuditLogsComponent implements OnInit {
    readonly list: ListService<GetAuditLogListDto>;
    private service;
    private store;
    datas$: Observable<AuditLogDto[]>;
    totalCount$: Observable<number>;
    selected: AuditLogDto;
    pageQuery: GetAuditLogListDto;
    filterForm: FormGroup;
    httpMethods: string[];
    httpStatusCodes: {
        code: number;
        message: string;
    }[];
    modalVisible: boolean;
    collapseActionStates: boolean[];
    collapseChangeStates: boolean[];
    sortOrder: string;
    sortKey: string;
    selectedTab: string;
    constructor(list: ListService<GetAuditLogListDto>, service: AuditLogsService, store: Store);
    ngOnInit(): void;
    private hookToQuery;
    openModal(id: string): void;
    httpCodeClass(httpStatusCode: number): string;
    httpMethodClass(httpMethod: string): string;
}
