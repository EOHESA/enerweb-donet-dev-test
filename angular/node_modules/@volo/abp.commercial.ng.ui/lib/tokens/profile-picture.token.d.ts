import { InjectionToken } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
export interface ProfilePictureImage {
    type: 'image' | 'icon';
    source: string | null;
}
export declare const PROFILE_PICTURE: InjectionToken<BehaviorSubject<ProfilePictureImage>>;
