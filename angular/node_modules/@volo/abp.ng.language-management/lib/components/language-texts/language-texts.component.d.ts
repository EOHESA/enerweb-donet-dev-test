import { ApplicationConfiguration, ConfigStateService, ListService } from '@abp/ng.core';
import { OnInit, Renderer2 } from '@angular/core';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { GetLanguagesTextsInput, LanguageTextDto } from '../../proxy/dto/models';
export declare class LanguageTextsComponent implements OnInit {
    readonly list: ListService<GetLanguagesTextsInput>;
    private store;
    private renderer;
    private configState;
    datas$: Observable<LanguageTextDto[]>;
    totalCount$: Observable<number>;
    columns: {
        field: string;
        header: string;
    }[];
    selected: LanguageTextDto;
    selectedIndex: number;
    pageQuery: GetLanguagesTextsInput;
    isModalVisible: boolean;
    modalBusy: boolean;
    languages: ApplicationConfiguration.Language[];
    resources: {
        name: string;
    }[];
    constructor(list: ListService<GetLanguagesTextsInput>, store: Store, renderer: Renderer2, configState: ConfigStateService);
    ngOnInit(): void;
    openModal(): void;
    closeModal(): void;
    private hookToQuery;
    edit(data: LanguageTextDto, index: number): void;
    save(next?: boolean): void;
    restore(): void;
}
