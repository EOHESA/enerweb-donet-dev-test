import { ApplicationConfigurationService, ConfigStateService, ListService } from '@abp/ng.core';
import { ConfirmationService } from '@abp/ng.theme.shared';
import { Injector, OnInit } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { Store } from '@ngxs/store';
import { Observable } from 'rxjs';
import { CultureInfoDto, GetLanguagesTextsInput, LanguageDto } from '../../proxy/dto/models';
export declare class LanguagesComponent implements OnInit {
    readonly list: ListService<GetLanguagesTextsInput>;
    private confirmationService;
    private store;
    private injector;
    private configState;
    private appConfigService;
    datas$: Observable<LanguageDto[]>;
    totalCount$: Observable<number>;
    cultures$: Observable<CultureInfoDto[]>;
    form: FormGroup;
    selected: LanguageDto;
    isModalVisible: boolean;
    modalBusy: boolean;
    flagIcons: string[];
    constructor(list: ListService<GetLanguagesTextsInput>, confirmationService: ConfirmationService, store: Store, injector: Injector, configState: ConfigStateService, appConfigService: ApplicationConfigurationService);
    private createForm;
    private hookToQuery;
    ngOnInit(): void;
    openModal(): void;
    add(): void;
    edit(id: string): void;
    save(): void;
    delete(id: string, name: string): void;
    setAsDefault(id: string): void;
}
